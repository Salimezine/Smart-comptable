name = "smart-comptable-teif-api"
main = "src/index.js"
compatibility_date = "2025-04-01"
compatibility_flags = ["nodejs_compat"]

[[d1_databases]]
binding = "DB"
database_name = "smart-comptable-d1"
database_id = "204848dc-3160-4eae-9f59-e96bf94c6f99"

[vars]
API_KEY = "smart-comptable-teif-2026"
# JWT secret utilisé pour signer les tokens d'auth (à remplacer par un secret)
JWT_SECRET = "sxcJ5lRjB0NeuWQfzVhZOKig64IpbtkUyw72XoEa3dvrYTC1"

# SECRETS (set with `wrangler secret put <NAME>`):
# TUNTRUST_PFX = "<base64 of your TunTrust .p12/.pfx file>"
# TUNTRUST_PFX_PASSWORD = "<pfx password>"
# TTN_SOAP_URL = "https://sites.impots.finances.gov.tn/efattura/WSFatturaServices.svc"
# TTN_USERNAME = "<ttn_username>"     # matricule fiscal (optionnel)
# TTN_PASSWORD = "<ttn_password>"     # mot de passe portail (optionnel)
# TTN_RELAY_URL = "https://your-vps-ip:3000/api/ttn-submit"   # VPS relay HTTP
# TTN_RELAY_TOKEN = "<auth_token>"                             # si AUTH_TOKEN configuré dans le relay
