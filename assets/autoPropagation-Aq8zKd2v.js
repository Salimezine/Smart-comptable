import{c as products,m as suppliers,t as clients,p as movements,h as recomputeStock}from"./erpStore-D44vY-Ui.js";
import{t as journalKey}from"./journalKey-CWT1IliS.js";
var norm=e=>(e||``).trim().toLowerCase().replace(/\s+/g,` `),mt=e=>(parseFloat(e)||0).toFixed(3),hh=e=>{let s=typeof e===`string`?e:JSON.stringify(e),h=5381;for(let i=0;i<s.length;i++)h=((h<<5)+h+s.charCodeAt(i))>>>0;return`x`+h.toString(16)};
function companyId(){try{return localStorage.getItem(`smart_comptable_current_id`)||``}catch{return``}}
function enabled(){try{let e=JSON.parse(localStorage.getItem(`smart_config`)||`{}`);return e.autoPropagation!==!1}catch{return!0}}
function readJournal(){try{let e=JSON.parse(localStorage.getItem(journalKey())||`[]`);return Array.isArray(e)?e:[]}catch{return[]}}
function tierName(line){let r=line?.fournisseur||line?.client||line?.libelle||line?.designation||``;return String(r).trim()}
function pieceLines(piece){return Array.isArray(piece.lines)?piece.lines:[piece]}
function productForLine(company,line){let q=norm(line.designation||line.libelle||line.nom||``);if(!q)return null;let p=products.getAll(company);return p.find(e=>norm(e.designation||e.nom||e.reference||``)===q)||p.find(e=>{let t=norm(e.designation||e.nom||e.reference||``);return t&&(t.includes(q)||q.includes(t))})||null}
function qtyFor(company,line,amount,type){let q=parseFloat(line.quantite);if(q>0)return q;let p=productForLine(company,line);let price=p?parseFloat(type===`sortie`?(p.prix_vente_ht||p.prix_achat_ht||0):(p.prix_achat_ht||p.prix_vente_ht||0)):0;if(price>0){let n=Math.round(amount/price);return n>0?n:1}return 1}
function mvtId(company,piece,type,productId){return`mv_${hh(`${company}|${piece.numeroPiece||piece.id}|${type}|${productId}`)}`}
function prodId(company,name){return`pr_${hh(`${company}|${name}`)}`}
function ensureTier(company,line,store,kind){let n=tierName(line);if(!n)return null;let t=store.getAll(company).find(e=>norm(e.nom)===norm(n));if(!t)t=store.upsert(company,{nom:n,code:store.generateCode(company,kind),actif:!0,source:`journal`});return t}
function derivePiece(company,piece){let lines=pieceLines(piece),out={products:[],movements:[]};let sup=lines.find(l=>/^(40|401|421)/.test(String(l.compte||``))),cli=lines.find(l=>/^(41|411|412)/.test(String(l.compte||``)));if(sup)ensureTier(company,sup,suppliers,`F`);if(cli)ensureTier(company,cli,clients,`C`);let ven=lines.some(l=>/^7/.test(String(l.compte||``))&&(parseFloat(l.credit)||0)>0),ach=lines.some(l=>/^6/.test(String(l.compte||``))&&(parseFloat(l.debit)||0)>0);if(ach||ven){let type=ven?`sortie`:`entree`,stockLines=lines.filter(e=>/^[67]/.test(String(e.compte||``)));for(let l of stockLines){let p=productForLine(company,l);let amount=type===`entree`?(parseFloat(l.debit)||0):(parseFloat(l.credit)||0);if(!p&&type===`entree`){let name=norm(l.designation||l.libelle||`Article`)||`Article`;let ref=(l.designation||l.libelle||``).trim().slice(0,30);p=products.upsert(company,{id:prodId(company,name),designation:name,reference:ref,prix_achat_ht:amount,prix_vente_ht:amount,taux_tva:19,stock_actuel:0,stock_mini:0,code:products.generateCode(company,`P`),actif:!0,source:`journal`});out.products.push(p.id)}if(p){let q=qtyFor(company,l,amount,type);if(q>0){movements.upsert(company,{id:mvtId(company,piece,type,p.id),product_id:p.id,designation:l.designation||l.libelle||p.designation,type,quantite:q,prix_unitaire_ht:parseFloat(l.debit||l.credit)||0,date:piece.date||new Date().toISOString().slice(0,10),reference:piece.numeroPiece||piece.numero||``,source:`journal`,company_id:company});out.movements.push(mvtId(company,piece,type,p.id))}}}}return out}
function recomputeSoldes(company){let journal=readJournal(),supMap={},cliMap={};for(let l of journal){let c=String(l.compte||``);if(/^(40|401|421)/.test(c)){let n=tierName(l);if(n){let k=norm(n);supMap[k]=(supMap[k]||0)+((parseFloat(l.credit)||0)-(parseFloat(l.debit)||0))}}if(/^(41|411|412)/.test(c)){let n=tierName(l);if(n){let k=norm(n);cliMap[k]=(cliMap[k]||0)+((parseFloat(l.debit)||0)-(parseFloat(l.credit)||0))}}}for(let t of suppliers.getAll(company)){if(t.source===`journal`){let k=norm(t.nom);suppliers.upsert(company,{...t,solde:mt(supMap[k]||0)})}}for(let t of clients.getAll(company)){if(t.source===`journal`){let k=norm(t.nom);clients.upsert(company,{...t,solde:mt(cliMap[k]||0)})}}}
function reconcile(){if(!enabled())return;let cid=companyId();if(!cid)return;let journal=readJournal(),group={};for(let e of journal){let k=e.numeroPiece||e.id;if(k){if(!group[k])group[k]=[];group[k].push(e)}}let touched=new Set();for(let k of Object.keys(group)){let piece={id:k,numeroPiece:k,date:group[k][0]?.date,lines:group[k]};let res=derivePiece(cid,piece);res.products.forEach(id=>touched.add(id));res.movements.forEach(id=>{let mv=movements.getById(cid,id);if(mv)touched.add(mv.product_id)})}for(let id of touched)recomputeStock(cid,id);recomputeSoldes(cid);for(let p of products.getAll(cid)){if(p.source===`journal`&&!movements.getAll(cid).some(m=>m.product_id===p.id)&&(!p.stock_actuel||p.stock_actuel<=0))products.delete(cid,p.id)}}
let timer=null;
function schedule(){if(!enabled())return;clearTimeout(timer);timer=setTimeout(reconcile,600)}
function boot(){if(typeof window===`undefined`)return;window.addEventListener(`journal:updated`,schedule);window.addEventListener(`storage`,e=>{if(e.key===journalKey()||(e.key||``).startsWith(`erp_`))schedule()});if(document.readyState===`complete`||document.readyState===`interactive`)setTimeout(reconcile,800);else window.addEventListener(`load`,()=>setTimeout(reconcile,800))}
boot();
export{reconcile as r,schedule as s};
